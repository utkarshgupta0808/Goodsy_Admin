<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.goodsy.goodsyadmin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.ItemInfoActivity"></activity>
        <activity android:name=".activities.ShopListItemActivity" />
        <activity android:name=".activities.RejectedShopsActivity" />
        <activity android:name=".activities.AcceptedShopsActivity" />
        <activity android:name=".activities.ShopInfoActivity" />
        <activity android:name=".activities.PhotoPreviewActivity" />
        <activity android:name=".activities.ItemListActivity" />
        <activity android:name=".activities.ShopListActivity" />
        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".activities.ForgotPassActivity" />
        <activity android:name=".activities.WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>